streamlit>=1.40.0
openai>=1.30.0
python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0

# Reads iPhone .HEIC photos. Without it, HEIC files are skipped.
pillow-heif>=1.0.0

# Used by src/graph.py, which wires the same tools into a LangGraph workflow.
langgraph>=0.2.0
langchain-core>=0.2.0

# Tests
pytest>=8.0.0
